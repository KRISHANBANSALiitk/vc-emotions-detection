from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A ML project which detect emotions from text.',
    author='IIT Kanpur',
    license='MIT',
)
